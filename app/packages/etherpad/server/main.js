// import { Meteor } from 'meteor/meteor'
// import { EtherpadControllerInstance } from './EtherpadController'

/* console.log('EtherpadController.createGroup()')
const createGroupSync = Meteor.wrapAsync(EtherpadControllerInstance.createGroup.bind(EtherpadControllerInstance))
const groupId = createGroupSync()
console.log('EtherpadController.createGroupPad(' + groupId + ', \'helloWorld\')')
const createGroupPadSync = Meteor.wrapAsync(EtherpadControllerInstance.createGroupPad.bind(EtherpadControllerInstance))
const groupPadId = createGroupPadSync(groupId, 'helloWorld')
console.log('EtherpadController.createAuthor(\'testId\', \'testName\')')
const createAuthorSync = Meteor.wrapAsync(EtherpadControllerInstance.createAuthor.bind(EtherpadControllerInstance))
const authorId = createAuthorSync('testId', 'testName')
console.log(authorId)
console.log('EtherpadController.createPadSession()')
const createPadSessionSync = Meteor.wrapAsync(EtherpadControllerInstance.createPadSession.bind(EtherpadControllerInstance))
const sessionId = createPadSessionSync(groupId, authorId, new Date().getTime() + (60 * 60 * 24))
console.log(sessionId)*/
